import styled from "styled-components"

export const Container = styled.section`
  width: 100%;
  height: 100lvh;

  video {
    width: 100%;
    height: 100lvh;
    object-fit: cover;
    position: fixed;
  }
`
